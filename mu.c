#include<stdio.h>
include<conio.h>
int main()
{
    double number;

    printf("Enter a positive num: ");
    scanf("%lf", &num);

    if (number <= 0.0)
    {
        if (number == 0.0)
            printf(\n"You entered 0.");
        else
            printf(\n"You entered a nega num:");
    }
    else
        printf(\n"You entered a positive num:");
    return 0;
